import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    userInfoWrap: {
        flexDirection: 'row',
        padding: 10,
        backgroundColor: 'white',
        alignItems: 'center'
    },
    avatarUserContainer: {
        marginRight: 10
    },
    profileText: {
        marginTop: 10
    },
    listWrap: {
        marginTop: 10
    }
});
