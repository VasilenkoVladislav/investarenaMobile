import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    postLikeContainer: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    postLikeText: {
        marginHorizontal: 5
    },
    commentLikeText: {
        fontSize: 10,
        marginHorizontal: 5
    }
});
