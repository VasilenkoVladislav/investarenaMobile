import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row'
    },
    imageContainer: {
        marginRight: 5
    },
    validityText: {
        fontSize: 10,
        color: '#999'
    },
    validityTime: {
        fontWeight: 'bold',
        fontSize: 12,
        color: 'black'
    }
});
