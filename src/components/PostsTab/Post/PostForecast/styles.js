import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row'
    },
    imageContainer: {
        marginRight: 5
    },
    validityText: {
        fontSize: 8,
        color: '#999'
    },
    validityTime: {
        fontSize: 12
    }
});
