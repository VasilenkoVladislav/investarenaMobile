import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    profitText: {
        fontSize: 10,
        color: '#999',
        marginRight: 5
    },
    profitValue: {
        fontSize: 10
    }
});
